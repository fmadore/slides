# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Python (the dev server / font helper)
__pycache__/
*.pyc

# Editor
.vscode/
.idea/

# Claude Code (local preview dev-server config, not shared)
.claude/launch.json

# Local build previews + CI-installed browser-check dependencies
_site/
node_modules

# CI cache for generated PDFs/social cards
.extras-cache/

# Local output of tools/visual-diff.mjs (the before/after guard for design passes)
visual-diff/

# Locally generated deck extras (CI builds these into _site)
talks/*/slides.pdf
talks/*/social-card.png
talks/*/.extras-hash
/social-card.png

# Impeccable design skill: machine-local hook consent (shared config .impeccable/config.json IS committed)
.impeccable/config.local.json
