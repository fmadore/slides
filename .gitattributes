# Normalise text to LF in the repo; let Git pick checkout EOL per platform.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.woff2 binary
*.png   binary
*.webp  binary
*.jpg   binary
*.jpeg  binary
*.pdf   binary
